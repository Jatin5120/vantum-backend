/**
 * Audio Processing Constants
 * Centralized audio configuration values
 */

/**
 * Target sample rate for all audio processing
 * All audio will be resampled to this rate before sending to Deepgram
 */
export const OUTPUT_SAMPLE_RATE = 16000;
